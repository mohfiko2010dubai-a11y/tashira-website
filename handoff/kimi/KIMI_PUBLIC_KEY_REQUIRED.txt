Kimi must provide exactly one public SSH key in OpenSSH format, preferably:

ssh-ed25519 <base64-public-key> <non-secret-comment>

Do not provide a private key, passphrase, password, token or certificate in chat.
The public key will be installed only for the revocable Staging account `kimi-deploy`.
