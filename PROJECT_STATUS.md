# TASHIRA Project Status

Last verified: 2026-08-27

## Current state

- The internal Rule Registry now has a read-only lifecycle projection and Operations presentation at `/staff/operations/regulatory-changes`, deployed on isolated Staging at exact SHA `04ac439238de0d8eb754ae0cb8aefe45067816f7`. It exposes immutable version transitions, rule version/layer/classification, source authority review state and source evidence without adding rule activation controls or finance/secret fields. Direct Staging repository E2E returned the three synthetic lifecycle events, denied an actor without `rule.read`, and exposed zero finance/secret fields; the temporary runner was deleted. The browser-equivalent public route is HTTP 200, while `REGULATORY_WATCHER` remains OFF so no staff/customer feature was activated. TypeScript, full ESLint, 665 tests (19 environment-gated skips), client/static assets and server bundle pass; CI run `33065502125` is green. Production remains unchanged.

- Rule Registry lifecycle persistence and its internal API are complete and verified on isolated Staging at exact SHA `dff85185c04a22662a79ace67f84ca7fb5d88e41`, while remaining closed behind `REGULATORY_WATCHER`. Migration `041` adds append-only DRAFT/review/approval/activation/retirement/rejection evidence with request fingerprints and update/delete tamper guards. Imports can create only immutable DRAFT versions; transitions enforce separate propose/review/activate permissions, optimistic expected-status checks, command idempotency with changed-replay rejection, source-authority approval before approval/activation, one-active-version conflict prevention, and a server-controlled Production owner gate that cannot be supplied by the API client. Verified rollback backup `/var/backups/tashira-staging/20260827T105133Z-rule-lifecycle-041-predeploy` passed SHA-256, gzip and schema-content checks before Migration `041` was applied only to `tashira_staging`. A scoped repository E2E created one synthetic INTERNAL version, proved DRAFT import/exact replay, changed-replay and stale-transition rejection, and finished DRAFT → UNDER_REVIEW → REJECTED with exactly three immutable events, zero approvals and zero active versions. The temporary runner was deleted; `REGULATORY_WATCHER` and Controlled Writes remain OFF. TypeScript, full ESLint, 664 tests (19 environment-gated skips), client/static assets and server bundle pass; Staging local/public health are HTTP 200. Production remained HTTP 200 at unchanged SHA `3d595412e8acab08dc4c019292892967a1fe1792`; Production and main/master remain unchanged.

- Source-authority governance now has an internal RBAC API backed by the single Migration `040` evidence store. `rule.read` lists sources and their latest classification event; `rule.review` records `APPROVED`, `REJECTED` or `CHANGES_REQUIRED` using a server-derived actor/time, command UUID idempotency, source-row locking and expected-latest-event concurrency. Unknown client authority fields and internal errors are sanitized, and the entire API remains closed behind `REGULATORY_WATCHER`. Exact SHA `a140ac9a6e98f20a1077a5574e69a4be276112cf` is deployed on Staging. A scoped repository E2E wrote exactly one immutable `REJECTED` event for an existing synthetic `INTERNAL` source, proved exact replay, changed-command conflict and stale-version rejection, and proved that a commercial source cannot be approved as `OFFICIAL`; zero `APPROVED` events exist. The temporary runner was deleted, the watcher/customer flags and Controlled Writes remain OFF, and local/public/browser routes are HTTP 200. TypeScript, full ESLint, 659 tests (19 environment-gated skips), client/static assets and server bundle pass. Production remained HTTP 200 at unchanged SHA `3d595412e8acab08dc4c019292892967a1fe1792`; Production and main/master remain unchanged.

- Official-source authority governance is now enforced by the actual MySQL Active Rule provider rather than only at import validation. Migration `040` adds versioned append-only source-classification decisions with actor, reason and policy-version evidence; update/delete tampering is rejected by MySQL triggers. The provider loads an `OFFICIAL` rule only when the latest source decision is `APPROVED`, the authority type belongs to the owner-approved ICP/GDRFA/UAE-government hierarchy, the policy version is supported and the source remains valid HTTPS evidence. Legacy sources and rule history are preserved without backfill or invented classification, but fail closed until reviewed. Verified Staging backup `/var/backups/tashira-staging/20260827T102332Z-source-authority-040-predeploy` passed hashes and gzip validation before applying Migration `040`; source counts remained `5` total / `4` historically labeled official, zero authority decisions were invented, and therefore zero active rules currently qualify as governed official evidence. Exact SHA `3b46284dc633f7451653e6367f69e6a8fd57e4c8` is deployed on Staging, PM2 and browser-equivalent local/public routes are HTTP 200, customer-facing flags and Controlled Writes are OFF. Existing authorized internal Read Model and Team test scopes were preserved. TypeScript, full ESLint, 654 tests (19 environment-gated skips), client/static assets and server bundle pass. Production remained online at unchanged SHA `3d595412e8acab08dc4c019292892967a1fe1792`; Production and main/master remain unchanged.

- AI and official-source governance now fail closed at the reusable runtime boundaries. A versioned `AI_ADVISORY_BOUNDARY_V1` allows only extraction, pre-screening and summary tasks and explicitly rejects AI eligibility decisions, rule activation and final submission outcomes; the existing document and ticket pre-screeners now pass through that boundary while preserving human/deterministic final authority. Rule imports now require the versioned `UAE_OFFICIAL_SOURCE_POLICY_V1` authority category and reject commercial, blog, forum or social evidence mislabeled as `OFFICIAL`, while retaining HTTPS, draft-only import, research validation and human-review protections. Exact SHA `fa0f1524f22a3fa0eb3c410c464d6bc2923479ee` was deployed on isolated Staging: database identity was `tashira_staging`, customer-facing flags and Controlled Writes remained OFF, PM2 was online and local/public health were HTTP 200. No migration was required and no current rule, evaluation, customer feature or database row changed. TypeScript, full ESLint, 650 tests (19 environment-gated skips), client/static assets and server bundle pass. Production remained online at unchanged SHA `3d595412e8acab08dc4c019292892967a1fe1792`; Production and main/master remain unchanged.

- Governed requirement classifications now represent `OPTIONAL` explicitly alongside `OFFICIAL`, `OPERATIONAL`, `CONDITIONAL` and non-customer-visible `INTERNAL`. This closes the ticket-policy representation gap without making return/onward/round-trip evidence globally required: the generic ticket definitions remain inactive DRAFT/CONDITIONAL, and only an approved rule or governed operational definition can require one. Customer projection distinguishes authority-required, TASHIRA-processing, may-be-required and optional wording. Migration `039` changes only the three relevant MySQL enums; it passed isolated MySQL 8.4 clean/legacy rehearsal and was applied to `tashira_staging` after verified backup `/var/backups/tashira-staging/20260827T100135Z-optional-039-predeploy`. Protected application/catalog/instance counts remained identical and zero definitions were changed to `OPTIONAL`. Exact SHA `294ff5085eae6ec6b7973621efffe217206179d9` is deployed on Staging; PM2, local/public Staging and the policy UI hard-refresh are HTTP 200, Controlled Writes and customer Operations flags remain OFF. TypeScript, full ESLint, 632 tests (19 environment-gated skips), client/static assets and server bundle pass. Production read-only health is HTTP 200; Production and main/master remain unchanged.

- Submission Policy governance now has a discoverable internal Operations UI at `/staff/operations/policies`, deployed on isolated Staging at exact SHA `2ce453315fa2d283fe4064f88a0633defe24db08`. It reads typed MySQL policy/history projections, shows the active `OPERATIONAL` policy and immutable lifecycle evidence, exposes configurable proposal thresholds, and derives proposal/review/activation controls exclusively from server-side RBAC capabilities. Operations users without the required permissions receive no mutation controls, while the API rejects anonymous access with HTTP 403. Proposal, review, approval, activation and supersession remain distinct audited lifecycle actions; no policy was created or activated during validation, exactly one active policy remains, Controlled Writes and customer Operations flags remain OFF, and official eligibility rules remain separate. Browser-equivalent hard refresh of the new route, PM2 and local/public Staging health are HTTP 200. TypeScript, full ESLint, 631 tests (19 environment-gated skips), client/static assets and server bundle pass. Production read-only health is HTTP 200; Production and main/master remain unchanged.

- Travel-date changes are transactionally coupled to the existing customer Travel Group update path and verified on isolated Staging at exact SHA `7596220fdf37aada91f720d2583df389cb60647f`. Migration `038` is complete in `tashira_staging`: the evidence table and both update/delete immutability triggers are present. The verified pre-migration rollback backup is `/var/backups/tashira-staging/20260827T093007Z-travel-date-038-predeploy`. Scoped synthetic MySQL E2E proved a pre-authority date change creates one new schedule, one immutable change event, one audit event and one command event; exact replay is idempotent; stale version is rejected; update/delete tampering is rejected; and a post-authority change creates a new `HUMAN_REVIEW_REQUIRED` snapshot without rewriting history. Staging local/public and read-only Production health are HTTP 200. TypeScript, full ESLint, 628 tests (19 environment-gated skips), client/static assets and server bundle pass. Production and main/master remain unchanged.

- Operational submission-policy administration now has a strict internal RBAC API on the feature branch. `rule.read`, `rule.propose`, `rule.review` and `rule.activate` independently gate list/history, proposal, review/approval and activation; actor identity/time are server-derived, commands reject unknown client authority fields, optimistic record versions prevent silent overwrite, lifecycle events are append-only, and activating a new approved version transactionally supersedes an existing active version. No Operations Employee receives mutation permission through the existing role templates. TypeScript, full ESLint, 623 tests (19 environment-gated skips), client/static assets and server bundle pass. This administration SHA is not yet deployed to Staging; the current owner-approved V1 policy and scheduler runtime remain healthy there at `081071a08bb24f14bcee6102ce8f1694eadfe9fe`. Production and main/master remain unchanged.

- Owner-approved Submission Scheduler V1 policy is implemented and verified on isolated Staging at exact SHA `081071a08bb24f14bcee6102ce8f1694eadfe9fe`. Official entry-validity evidence is a distinct contract from the versioned TASHIRA `OPERATIONAL` submission policy. Deterministic boundaries are covered at 46/45/21/20/8/7/4/3/0 days: future travel is scheduled, 21–45 days is recommended, 8–20 is ready only when all readiness prerequisites pass, 4–7 is urgent, and 0–3 fails to human review. Alert thresholds come from the same active governed policy (`DUE_SOON=14`, `URGENT=7`, `DUE_TODAY=0`) instead of environment variables. Verified rollback backup `/var/backups/tashira-staging/20260827T085829Z-policy-037-predeploy-verified` passed SHA-256, gzip and storage/runtime checks before Migration `037` added two policy/audit tables, two append-only triggers and the new immutable schedule states. One owner-approved Staging policy has a complete DRAFT → REVIEW → APPROVED → ACTIVE four-event history; existing Team 7/13 internal Scheduler/Travel scopes remain unchanged, Controlled Writes stays OFF, and no customer flag was activated. TypeScript, full ESLint, 621 tests (19 environment-gated skips), client/static assets and server build pass. Staging and read-only Production health are HTTP 200; Production and main/master remain unchanged.

- The Unified Dynamic Interview now exposes the complete authenticated read contract (`start`, `resume`, current question, eligibility, requirements, upload requirements, scheduler result and review summary) as thin projections over the single canonical persisted-state function. No new query, eligibility, requirement, scheduling or ownership path was introduced. Every projection inherits the existing customer application capability check and closed application-scoped feature flags. Exact SHA `5afea33e9e8bdf54e417c0b54f4f383e6659e55c` is deployed on isolated Staging; a repeatable synthetic E2E passed lifecycle equivalence, all eight authenticated projections, anonymous denial and finance isolation. TypeScript, full ESLint, 608 tests (19 environment-gated skips), client/static assets and server build pass. The application-scoped customer flags were enabled only for `TSH-STG-DYN-INDIVIDUAL` during the test and returned to OFF; `OPERATIONS_CONTROLLED_WRITES` and `OPERATIONS_EMAIL_AUTOMATION` are OFF. The pre-existing Team 7 `VISA_RULES_EVALUATION` scope remains an internal read/evaluation scope and is not customer-facing. PM2/local/public Staging and read-only Production health are HTTP 200. Production and main/master remain unchanged.

- Provider-independent Operations Email Automation persistence is complete on isolated Staging, while all delivery remains disabled. Migration `036` adds immutable dispatch identity and append-only delivery-event evidence bound by trigger to an existing timeline event owned by the same application. The MySQL adapter derives and stores only a SHA-256 of the current application contact, never plaintext recipient data, and queues exactly one `QUEUED` event with transactional deduplication, changed-command conflict detection and concurrent duplicate recovery. The closed `OPERATIONS_EMAIL_AUTOMATION` flag gates the runtime; there is no API route, scheduler, template-policy mapping, Resend call, SMTP code or provider credential. A verified root-only rollback backup at `/var/backups/tashira-staging/20260826T183037Z-operations-email-036-predeploy` passed SHA-256, gzip and tar checks before Migration `036` was applied only to `tashira_staging`. The first scoped test failed safely before queue insertion because the service leaked its circular runtime context across the repository boundary; the flag returned OFF, the command boundary was corrected and regression-tested, and exact SHA `a7cb809b5951a5fd1bcce6bc2c5b68ab65da6af8` was deployed canonically. The retry passed queue, replay, changed-command conflict, recipient-hash-only persistence, database immutability and unchanged legacy outbound-provider ledger; one synthetic dispatch/event remains as immutable TEST evidence and no email was sent. TypeScript, full ESLint, 605 tests (19 environment-gated skips), client/static assets and server build pass. The flag is OFF; local/public Staging and read-only Production health are HTTP 200. Approved event-trigger/template policy and provider dispatch remain true owner gates. Production and main/master remain unchanged.

- Authenticated Case Chat handoff is now complete on isolated Staging without creating a second chat store or outbound messaging path. The customer mutation reuses the canonical MySQL case bundle, Visa Assistant grounding and existing Support Inbox tables; it requires the owned customer application session, an assigned Operations team, every applicant's current immutable evaluation and three application-scoped closed flags. Only an allowlisted question key and finance-free evidence summary are persisted. The server creates an unassigned support thread, one sanitized inbound message and one append-only audit event transactionally, with request fingerprint idempotency and concurrent duplicate recovery; changed replays fail with conflict. TypeScript, full ESLint, 601 tests (19 environment-gated skips), client/static assets and server build pass. Exact SHA `67d2beb311b88d94030e49bed9d6bb9b970c0660` is deployed to isolated Staging. Scoped synthetic E2E passed create, exact replay, changed-replay conflict, anonymous denial, exactly-one message/audit evidence and finance isolation. `CASE_CHAT_HANDOFF`, `VISA_ASSISTANT`, `CUSTOMER_OPERATIONS_PORTAL` and `OPERATIONS_CONTROLLED_WRITES` are all OFF afterward; one clearly synthetic unassigned support thread remains as immutable test evidence. Local/public Staging and read-only Production health are HTTP 200. No provider message, customer email or external notification was sent. Production and main/master remain unchanged.

- The authenticated Customer Visa Assistant now has one application-owned tRPC runtime backed by the canonical MySQL Operations case bundle and the existing finance-minimized Customer Operations Portal projection. It supports only five allowlisted case-evidence questions (status, requirements, travel groups, submission timing and missing documents); unknown or unapproved knowledge fails closed to `HUMAN_REVIEW_REQUIRED`. The endpoint requires the existing customer application session, rejects anonymous and cross-application access before returning data, and is gated by the application-scoped `VISA_ASSISTANT` plus `CUSTOMER_OPERATIONS_PORTAL` flags. TypeScript, ESLint, 599 tests (19 environment-gated skips), client/static assets and server build gates pass. Exact SHA `7a7e0882a1e16889630541b71fe0f53ce7e4de84` is deployed to isolated Staging. Scoped synthetic E2E on `TSH-OPS-STEP2-FAMILY` passed all five answers, unknown-knowledge human-review fallback, anonymous/cross-application denial and finance/storage isolation. Cleanup detected one residual Team 7 `OPERATIONS_CONTROLLED_WRITES` scope left by an earlier Step 3 test; it was explicitly disabled, and all three targeted flags are now OFF. PM2/local/public Staging and read-only Production health are HTTP 200. Approved official FAQ/policy/procedure content and any conversational provider remain owner/external gates; no knowledge or answer was invented. Production and main/master remain unchanged.

- The governed Customer Visa Pre-Check now has a responsive public route at `/visa-pre-check` and consumes only the existing rate-limited, server-authoritative Active Rule API behind the closed `CUSTOMER_PRECHECK` flag. The UI collects a small non-sensitive profile, labels results as likely guidance or human review, exposes rule-derived document codes and source verification state, and always states that the result is not approval or a government guarantee. Full TypeScript, ESLint, 596 tests (19 environment-gated skips), client/static asset and server build gates pass. Exact SHA `ed3ed587cd5f60e96c5378c34992790c2b4ef455` is deployed to isolated Staging; a temporary synthetic global flag produced `LIKELY_ELIGIBLE` only from the approved `STAGING_TEST_DYNAMIC` fixture, preserved finance/storage isolation, and returned to OFF. Browser-equivalent route navigation and the lazy asset are HTTP 200/present, while interactive Browser-plugin sign-off remains externally blocked. All customer Operations flags are OFF, PM2/local/public Staging and read-only Production health are HTTP 200, and no Production rule or customer behavior changed. Production and main/master remain unchanged.

- Secure Visa Delivery is complete on isolated Staging at exact SHA `6c09f3f97a87f1829b2839522bc6ecba23ca43ff` behind the closed `VISA_DELIVERY` flag. Migration `035` adds applicant-owned, append-only document security-scan evidence and immutable delivery packages guarded by six ownership/immutability triggers. The single MySQL adapter requires the latest scan result to be `PASSED`, an uploaded visa document owned by the same application/applicant, trusted Operations `document.review` scope, replay-safe command identity and append-only audit evidence. Customer APIs require the existing owned application session and return only safe package metadata or a 15-minute signed storage URL; no storage path, public document URL, finance, Stripe or supplier-cost field is returned. TypeScript, full ESLint, 595 tests (19 environment-gated skips), client/static assets and server build pass. Verified rollback backup `/var/backups/tashira-staging/20260826T174856Z-visa-delivery-035-predeploy` passed SHA-256, gzip and tar checks before the migration and exact-SHA canonical deployment. Scoped synthetic E2E on `TSH-OPS-STEP2-FAMILY` passed preparation, replay, applicant isolation, wrong-team denial, owned customer listing, signed file download, anonymous denial, finance isolation and exactly-one audit evidence; attempted scan/delivery mutation was rejected by MySQL. The application-scoped flag returned to OFF, temporary staff/grants were removed, and one explicitly synthetic visa document/scan/delivery remains as immutable TEST evidence, increasing the expected document count to 124. PM2/local/public Staging and read-only Production health are HTTP 200. A real approved malware-scanning provider and policy remain an owner/external integration gate; no scan provider or Production behavior was invented. Production and main/master remain unchanged.

- Regulatory Change Center persistence, RBAC API and internal review UI are now complete on isolated Staging behind the closed `REGULATORY_WATCHER` flag. The repository compares only two successful snapshots belonging to the same active `OFFICIAL` Rule Registry source, rejects unchanged/out-of-order evidence, computes affected active applications from their current selected evaluation's matched source snapshot, and persists immutable proposal/impact evidence plus replay-safe optimistic human-review events. `APPROVED` means only that the proposal passed review: the data model, API and UI contain no rule-activation path and never rewrite historical evaluations. Migration `034` adds three tables and six append-only/identity triggers. Full TypeScript, ESLint, 587 tests (19 environment-gated skips), client/static asset and server build gates pass. Verified rollback backup `/var/backups/tashira-staging/20260826T173205Z-regulatory-034-predeploy` preceded Migration `034` and canonical deployment of exact SHA `af231bc53dc9926119173ec353af071f8cf0b1b3`. Scoped synthetic E2E used only an `.invalid` source and passed proposal replay, stale-version denial, explicit permission denial, three-event review history, finance isolation and exact before/after Rule Registry counts with zero automatic activation; the synthetic source was made `INACTIVE` and the flag returned to `NO`. Browser-equivalent HTML navigation to `/staff/operations/regulatory-changes` is HTTP 200 and its lazy asset exists; interactive browser sign-off remains blocked by the desktop Browser plugin trusted-path error. Official-source connectors and authorized real regulatory content remain true owner/external gates. Production and main/master remain unchanged.

- Provider-independent Typing Pack and Authority Query runtime persistence is now complete on isolated Staging behind closed flags. One MySQL adapter reuses the trusted Operations case/evaluation providers: Typing Pack generation requires the same application's current selected applicant evaluation plus exactly one effective `APPROVED` template, derives only an explicit supported field allowlist, snapshots template/evaluation evidence immutably and remains draft-only for human review. Authority Query creation and transitions require `authority.record_submission`, trusted case scope, optimistic versions, replay-safe command IDs, append-only audit evidence and a safe external reference before `RESPONSE_RECEIVED`; no government portal, credentials or provider submission code exists. Migration `033` adds four tables and ten ownership/immutability triggers. The first DDL attempt correctly failed because the application user has no `CREATE` privilege and created zero tables; the migration was then applied through the local Staging-only administrative channel without broadening application privileges. Verified rollback backup `/var/backups/tashira-staging/20260826T171121Z-authority-output-033-predeploy` passed gzip and SHA-256 verification before exact SHA `e06d9b73f6e3ba5993c85e2952ca886a8fd90d1b` was deployed through the canonical native path. Scoped Team 7 synthetic E2E on TEST case `TSH-OPS-STEP2-FAMILY` passed Typing Pack replay, applicant ownership denial, Authority Query replay, required response reference, wrong-team denial, four-event lifecycle history and finance/storage isolation. Both flags returned to `NO`; one immutable synthetic pack/query and a ten-minute synthetic template remain as explicit TEST evidence. Protected counts remain 70 applications / 85 applicants / 123 documents and the pre/post document fingerprint is `53de34491f7c4cf3ef28852699e8a6d960a6d3671295a8ba348f8d87460c6666`. TypeScript, full ESLint, 581 tests (19 environment-gated skips), client/static assets and server build pass; PM2/local/public Staging and read-only Production health are HTTP 200. Owner-approved Typing Pack templates and the authority operating procedure/provider remain true owner gates. Browser UI E2E remains blocked before tab creation by the desktop Browser plugin trusted-path error. Production and main/master remain unchanged.

- Supplier SLA and escalation controls are now complete behind the closed `SUPPLIER_SLA` flag. One server-authoritative flow derives the assigned supplier and lead applicant's current selected route, requires exactly one explicit effective policy or fails closed, snapshots policy/deadline evidence immutably, and provides replay-safe optimistic `START`, `ACKNOWLEDGE`, `ESCALATE`, and `COMPLETE` actions with append-only audit history. Operations payloads contain supplier identity and timing only; no cost, margin, pricing, payment, Stripe or storage data is selected. Migration `032` passed MySQL 8.0.46 apply → exact 3-table/6-trigger verification → rollback → reapply, and the real transactional repository passed START/replay/stale-version/completion/audit/immutability tests on a disposable schema clone. TypeScript, full ESLint, 575 tests (19 environment-gated skips), client/static assets and server build pass. Verified Staging backup `/var/backups/tashira-staging/20260826T165022Z-supplier-sla-032-predeploy` preceded Migration `032` and canonical deployment of exact SHA `4787956738877083a5e7bec0c085e0712ebb30c5`. Scoped Team 7 E2E on TEST application `TSH-OPS-STEP2-FAMILY` passed start, replay, manager scope, acknowledgement, stale conflict, completion, finance/unauthorized/anonymous denial and finance/storage isolation. The automatic cleanup did not fire after the temporary runner exited, so a fail-closed verification detected the residual flag/role and forced cleanup immediately; `SUPPLIER_SLA` is OFF and temporary roles are zero. Protected counts remain 70 applications / 85 applicants / 123 documents, document fingerprint is `543b25bd12c8a515b769e976604f126713c678dd555519337f6e6d94df466b95`, and PM2/local/public Staging plus read-only Production health are HTTP 200. Business-approved supplier SLA policies remain an owner configuration gate; the synthetic E2E policy is time-limited and applies only to the tested supplier/route while the feature is OFF. Production and main/master remain unchanged.

- Support Inbox now has one transactional MySQL repository, trusted RBAC service, strict staff-only tRPC API and protected internal UI behind the closed `SUPPORT_INBOX` flag. Migration `031` supplies team-scoped optimistic thread heads plus append-only provider-deduplicated messages, internal notes and idempotent command/audit evidence. Claim/assign/reassign/state/note commands derive actor/time server-side, preserve a stable command key across UI retries, reject stale versions and validate assignment targets against active team/department scope. The workspace sends no outbound email and exposes no attachments, payment, pricing, supplier-cost, margin, profit, Stripe or storage fields. After the isolated MySQL 8.0.46 apply/repository/rollback/reapply rehearsal passed, a verified root-only Staging backup was created at `/var/backups/tashira-staging/20260826T161421Z-support-inbox-031-predeploy`, exact SHA `e5dd18909c8e90deaf48b4625e26ceef160a1b1d` was deployed, and Migration `031` was applied only to `tashira_staging`. Staging has the exact four tables and six append-only triggers; two real transactional repository tests passed against the isolated Staging database, including replay, stale-version rejection, audit evidence and attempted message-update rejection. A diagnostic error exposed the former Staging-only database credential in the task log, so it was immediately rotated server-side without printing the replacement; the root-only configuration backup is `/var/backups/tashira-staging/20260826T162429Z-db-credential-rotation`, `.env` remains mode 600, the new connection is verified and only `tashira-staging` was restarted. `SUPPORT_INBOX` remains OFF, no provider is connected, no inbound/outbound email is accepted, PM2/local/public Staging are HTTP 200, and protected counts remain 70 applications / 85 applicants / 123 documents. Browser UI sign-off is still blocked before tab creation by the desktop Browser plugin's trusted-path error. Full TypeScript, ESLint, 562 tests (17 environment-gated skips) and the complete client/static/server build pass. Production and main/master remain unchanged.

- The finance-minimized Operations Manager Dashboard is verified on isolated Staging at exact SHA `83a03203287a27c3f34b908a51853ac12db476c3`. One canonical MySQL analytics provider supplies only trusted operational case, assignment, readiness, schedule, rework and supplier-identity evidence; the server filters every candidate through the existing RBAC scope policy before aggregation. Scoped synthetic API E2E proved a Team 7 manager received exactly two permitted cases, an Operations Employee and anonymous caller were denied with 403, finance fields were absent, and browser-equivalent direct SPA navigation returned HTTP 200 with the expected lazy asset. Synthetic staff/grants were removed, protected counts remain 70 applications / 85 applicants / 123 documents, and the document fingerprint remains `f410306d5930425efdae9b5bb30db3da9cac4f7a17aff625ecae3181fd67d579`. TypeScript, full ESLint, 552 tests (15 environment-gated skips) and the complete client/static/server build pass. Rollback configuration evidence is at `/var/backups/tashira-staging/20260826T154653Z-manager-dashboard-predeploy`; PM2/local/public Staging and read-only Production public health are HTTP 200. Production and main/master remain unchanged.

- Unified Dynamic Interview applicant-requirement uploads are now complete through the canonical API/MySQL/Staging path at exact SHA `e0a2aa5fe574033651b1bbd784251d36d8839ce9`. A reproducibility defect was traced to a post-build checkout overwriting tracked `dist/boot.js`; the runtime and E2E harness are now deployed as one exact SHA with no later checkout. Migration `030` corrects the historical answer uniqueness contract so a legitimate `A → B → A` correction creates three immutable predecessor-linked events instead of failing with `ER_DUP_ENTRY`; real disposable MySQL 8.4.11 integration passed. Verified rollback backup `/var/backups/tashira-staging/20260826T152550Z-answer-transitions-030-predeploy` contains DB, storage, runtime configuration and Git evidence with passing SHA/gzip/tar checks. The scoped synthetic Staging E2E passed authorization, applicant isolation, owned upload/link, `MISSING → UPLOADED` readiness, replay idempotency, cross-application denial and finance isolation. Staging now contains 18 requirement instances and one synthetic requirement-document link; protected counts are 70 applications / 85 applicants / 123 documents and document fingerprint is `d1d6db6fa81ee296756e779df01b99d369431b893eef36a162432ddc3054d320`. All GLOBAL/APPLICATION customer flags and global Controlled Writes are OFF, PM2/local/public Staging are healthy at HTTP 200, and Production public health remained HTTP 200 read-only. Browser UI E2E remains blocked before tab creation because the desktop Browser plugin rejects its own trusted service path; no browser/session bypass was used. Production and main/master remain unchanged.

- The Unified Dynamic Interview customer document workspace now projects the current applicant-scoped document requirements from the selected immutable evaluation and authoritative active Requirement Catalog, then uploads through the existing owned storage/document APIs and links through the single transactional MySQL customer-write adapter introduced by Migration `029`. The API derives the application from the authenticated reference, keeps the feature fail-closed behind `DYNAMIC_REQUIREMENTS`, rejects unresolved catalog requirements, and returns no finance/Stripe fields. The responsive UI groups requirements by their owning applicant, suppresses upload controls after `UPLOADED`/`VALIDATED`/`WAIVED`, and never copies a document across applicants. TypeScript, full ESLint, 542 tests and the complete client/static/server build pass locally. Migration `029` remains unapplied to Staging and all customer feature flags remain OFF pending a fresh verified Staging backup, migration gate, exact-SHA deployment and scoped synthetic E2E. Production and main/master remain unchanged.

- Applicant-scoped dynamic requirement uploads now have a persistence foundation in inactive Migration `029`: an append-only link binds one existing owned document to the applicant's current selected requirement instance, records a SHA-256 integrity indicator, appends `UPLOADED` requirement evidence and stores restart-safe command idempotency without changing the legacy document/storage schema. The trusted MySQL adapter fails closed when the current evaluation/requirement is missing or the document is replaced, application-level or owned by another applicant. Clean and legacy disposable MySQL 8.4.11 databases passed apply → structural/trigger verification → rollback → reapply, and the real repository integration passed ownership, current-selection, idempotency and chronological `MISSING → UPLOADED` evidence. TypeScript, full ESLint, 539 tests and the complete build pass. Migration `029` is not applied to Staging and no API/UI or feature is activated yet. Production and main/master remain unchanged.

- The Unified Dynamic Interview shared travel-document workspace is verified on isolated Staging at exact SHA `8b2f8a93b15e0e8ed5160320bc6c0900974f30c0`. It exposes only existing shared-document coverage and lets the authenticated owner append same-application applicants through the canonical MySQL transaction; previous links remain immutable, no binary is copied and unknown types fail closed. Verified rollback backup `/var/backups/tashira-staging/20260826T140352Z-shared-documents-predeploy` contains the database, storage, runtime configuration and Git evidence with passing SHA/gzip checks. The scoped synthetic E2E created one PDF through the existing `storage.upload` + `document.create` APIs, linked it as a family booking, appended another applicant, denied cross-application linkage and preserved finance isolation; application-scoped customer flags returned to OFF. The expected synthetic document count is now 122 and shared-link count 4. PM2/local/public Staging are HTTP 200. Browser E2E remains externally blocked before tab creation because the desktop Browser plugin rejects its own trusted service path; no alternate session/security bypass was used. Production and main/master remain unchanged.

- The existing finance-minimized Customer Operations Portal now has a real lazy-loaded customer route at `/applications/:referenceNumber/status`. The page calls only the authenticated `customerOperations.portal` endpoint, displays the existing safe applicant/action/schedule/timeline projection, and fails closed with a generic recovery message unless the current customer session owns the reference and the application-scoped `CUSTOMER_OPERATIONS_PORTAL` flag is enabled. No supplier, finance, internal reason, storage path or staff data is added. TypeScript, full ESLint, 537 tests and the complete client/static/server build pass. Exact SHA `cfeb26c8c20f7a99a9215c96f33277a73ae0adea` is deployed to isolated Staging. A scoped synthetic family API E2E passed anonymous and cross-application denial, applicant projection, timeline projection and finance/internal-field isolation; the Portal flag returned to OFF afterward. PM2/local/public Staging remain HTTP 200. Production and main/master remain unchanged.

- The Unified Dynamic Interview now exposes one feature-gated, server-authoritative travel-party setup projection and customer UI for applicant profiles, lead-relative family relationships and versioned travel groups. The projection reuses the trusted MySQL Operations provider, returns no finance/Stripe fields, remains `null` while `DYNAMIC_REQUIREMENTS` is closed, and carries current applicant profile and travel-group versions into optimistic-concurrency writes. Add/edit applicant, relationship and travel-group actions use only the authenticated existing tRPC commands and refetch the canonical state after success; no parallel query or business-logic path was introduced. TypeScript, full ESLint, 536 tests and the complete client/static/server build pass. Exact SHA `6475fe176f70ff820294bd0c7af46ec402acc9ad` is deployed through the canonical path to isolated Staging after verified rollback backup `/var/backups/tashira-staging/20260826T131806Z-unified-party-ui-predeploy`. A scoped synthetic family API E2E passed anonymous/cross-application denial, applicant add/edit, relationship persistence, optimistic travel-group versioning, ownership isolation and finance isolation; the three application flags returned to OFF afterward. PM2/local/public Staging health is HTTP 200. Real browser E2E remains blocked before tab creation by the desktop Browser plugin rejecting its own trusted service path; no browser/session bypass was used. Production and main/master remain unchanged.

- Unified Dynamic Interview API E2E is green on isolated Staging at exact SHA `56dc88dc216fb892c01874f9d144236095c757e5`. The test proved anonymous/cross-application denial, applicant isolation, one immutable initial snapshot plus one superseding re-evaluation, replay idempotency, canonical travel projection and finance-field non-disclosure. Two persistence-boundary defects found during the gate are fixed without weakening the customer-safe error: governed JSON scalar decoding and canonical MySQL `DATE` normalization. PM2/local/public Staging health is HTTP 200. The three customer flags remain scoped only to the approved individual synthetic application and Controlled Writes remain OFF. In-app Browser E2E is pending because the desktop Browser plugin rejected its own trusted service path before opening a tab; no alternate browser/security bypass was used. Production/main/master are unchanged.

- Unified Dynamic Interview multi-scenario Staging E2E is complete at exact SHA `61511dd9017155f9c0b7c4c2fd40cca6c8457a63`. GCC residence, a four-member mixed-nationality family, an unresolved profile and an authoritative RULE_CONFLICT each passed authenticated governed-question progression, immutable per-applicant evaluation evidence, cross-application denial, applicant isolation and finance/Stripe non-disclosure. Family evidence proved BANK_STATEMENT remained exclusive to the Pakistani mother. The unresolved profile correctly used the authoritative `HUMAN_REVIEW_REQUIRED`/`UNRESOLVED_PROFILE` contract rather than an invented decision. Every scenario was enabled only at APPLICATION scope during its test and automatically disabled afterward; all five synthetic customer scopes are now OFF. Staging remains HTTP 200/200 and Controlled Writes were not changed.

- Completed Dynamic Interview answers now pass through one canonical evaluator for both customer projection and immutable MySQL evidence. The authenticated answer/correction path persists a replay-safe evaluation snapshot per applicant, links exact governed rule versions, records conflict evidence, appends a new current-selection event, and preserves the previous snapshot through `supersedes_evaluation_id`; deterministic trigger-event IDs permit safe recovery after a partial request retry. The Unified persistence adapter now fails closed if any applicant lacks a selected current evaluation instead of manufacturing a fallback. A staging-only preparation guard uses the existing transactional customer-write repository for synthetic family relationships and travel groups and refuses non-Staging identity or premature customer flags. Forced TypeScript, full ESLint, 528 tests and the complete client/static/server build pass. These changes are not yet deployed or activated; Production and main/master are unchanged.

- Unified Dynamic Interview review presentation is now connected to the customer route behind the existing closed application flags. It renders applicant-isolated eligibility, governed requirements, relationships, travel-party membership, submission timing, shared-document coverage gaps and manual-review blockers without finance or Stripe fields. Focused server-render tests prove one applicant's requirements do not appear inside another applicant's panel. The milestone also resolves the two existing React effect lint findings without disabling rules and makes the MySQL repository constructor compatible with `erasableSyntaxOnly`. Forced TypeScript, full ESLint, 527 tests and the complete client/static/server build pass. Exact SHA `f84039a73a9bf3ca21d8cd3514748f2e1265fb9a` is currently healthy on isolated Staging at HTTP 200/200; Migration `028` is structurally verified with its profile-version column, two evidence tables and four append-only triggers, protected counts remain 70 applications / 84 applicants / 121 documents, and all customer Dynamic Interview flags remain OFF. Production and main/master are unchanged.

- Customer Travel Group and Shared Document write integration is complete behind closed application flags. Transactional create/update derives and verifies every applicant against the owned application, validates primary/accompanying membership and dates through the canonical Travel Party validator, uses optimistic group versions, replaces membership atomically, and records immutable versioned command evidence. Shared-document linking validates the existing document plus every applicant, rejects type/cross-application conflicts, uploads no duplicate binary and adds only missing append-only links. Authenticated tRPC mutations expose create/update/link without trusting client ownership. Real MySQL 8.4.11 integration passed create/update, replay, stale conflict, cross-application denial and one-document/multi-applicant linkage. TypeScript, focused ESLint, 525 full-suite tests and Vite build pass. No Staging migration/deployment or feature activation has occurred; Production/main/master are unchanged.

- Customer Relationship persistence and API now support the explicit V1 customer vocabulary `SPOUSE`, `PARENT`, `CHILD`, `GUARDIAN` and `DEPENDENT` without collapsing meanings into `OTHER`. Migration `028` additively expands the canonical relationship enum; the domain model and trusted MySQL reader share the same values. The transactional command validates both applicant IDs inside the owned application, rejects self/cross-application links, preserves append-only relationship evidence and provides hash-checked idempotent replay. The authenticated mutation derives application/actor server-side and remains application-flag gated. MySQL 8.4.11 integration, TypeScript, focused ESLint and 525 full-suite tests pass. Migration/feature remain unapplied and OFF outside local rehearsal; Production/main/master are unchanged.

- Authenticated Add Applicant and Edit Applicant mutations now use the transactional customer-write repository under the same application capability plus application-scoped `DYNAMIC_REQUIREMENTS` gate. The server supplies the trusted application ID/actor, validates inputs, returns safe conflict messages, and never trusts a client role or application owner. Unified MySQL projection is intentionally deferred until no adaptive question remains, allowing a newly added applicant to complete required profile questions before a current immutable evaluation is required. API tests prove the provider stays closed when its flag is OFF and receives the owned application when ON. TypeScript, focused ESLint, 525 full-suite tests (13 environment-gated skips) and Vite build pass; no route is active externally and Staging/Production/main/master are unchanged.

- Customer applicant persistence now has a transactional MySQL repository for Add Applicant and Edit Applicant. It locks the owned application, derives the next trusted applicant index server-side, enforces application/applicant ownership, uses optimistic `profile_version`, records immutable profile snapshots, hashes commands for conflict-safe idempotent replay, and rolls back stale or cross-application writes. A real MySQL 8.4.11 disposable integration test passed creation, exact replay, conflicting replay rejection, edit history, stale-write rejection and cross-application denial. Migration `028` was revised to retain the command hash and completed local clean/legacy rollback/reapply; TypeScript and focused ESLint pass. No API endpoint or feature was activated and Staging/Production/main/master remain unchanged.

- Customer Interview write persistence now has an additive, inactive Migration `028` contract: optimistic `applicants.profile_version`, immutable applicant-profile evidence, and application-scoped idempotent command evidence for relationships, travel groups and shared-document links. Both evidence tables reject update/delete and preserve application/applicant foreign-key ownership; no business, payment or finance columns are introduced. MySQL 8.4.11 local disposable `tashira_ops_rehearsal_app` and `tashira_ops_rehearsal_legacy` completed apply → structural verification → rollback → reapply with the expected column, two tables and four triggers. Migration `028` is not applied to Staging; no write API or feature flag is active and Production/main/master are unchanged.

- The authenticated Dynamic Interview API now has a feature-gated persistence handoff to the existing trusted MySQL Operations case provider. A single adapter converts selected immutable eligibility snapshots, the current directed relationship graph, applicant-owned travel groups, current schedule snapshots and shared-document links into the Unified Interview Orchestrator contract; missing current evaluations or lead-relative relationships fail closed rather than being guessed. The provider is never queried while `DYNAMIC_REQUIREMENTS` is closed, so inactive/legacy customer behavior is unchanged. TypeScript, focused ESLint, 522 full-suite tests (12 understood environment-gated skips) and the production Vite build pass. The new API projection remains inactive and has not been deployed to Staging; all customer flags remain OFF and Production/main/master are unchanged.

- A closed-by-default Unified Dynamic Interview orchestration boundary now composes the existing server-authoritative Family Engine, applicant identities, governed Requirement Catalog, travel groups, shared-document ownership, immutable submission schedules and customer review into one canonical result. It fails closed on application/applicant/document/schedule ownership mismatch and duplicate current group/schedule identities, retains independent applicant eligibility and requirements, reports incomplete shared-document coverage without copying data between applicants, and hands the exact same immutable projection to the future Customer Portal boundary. Feature closure returns no customer model. Twenty-six focused family/travel/interview/orchestrator tests, TypeScript, focused ESLint and the production Vite build pass. This pure orchestration milestone is not connected to persistent runtime APIs or activated in Staging; all customer flags remain OFF and Production/main/master are unchanged.

- Dynamic Interview now produces one server-authoritative applicant Review model from the same current ACTIVE rules, current non-invalidated answers and ACTIVE governed Requirement Catalog used by the interview. Each applicant receives an isolated eligibility outcome, customer-safe message and catalog-backed requirement label/classification/explanation; unresolved requirement codes fail closed rather than exposing raw IDs or guessing meaning. NOT_RESEARCHED and RULE_CONFLICT are translated to safe specialist-review wording. The responsive customer UI renders this model without recreating rule logic. Twelve focused state/service/router tests, TypeScript and focused ESLint pass. Family relationships, travel groups, scheduler, shared uploads and Portal handoff remain closed and unactivated until their unified runtime is complete.

- Exact feature SHA `c4754781af529ad8ed23a89a2e1fee3e918fe0ad` is deployed to isolated Staging through the canonical native path. Verified rollback backup `/var/backups/tashira-staging/20260826T101751Z-dynamic-interview-rules-predeploy` passed gzip/SHA preparation before mutation. Six explicitly non-regulatory `STAGING_TEST` rules completed DRAFT → UNDER_REVIEW → APPROVED → ACTIVE under the immutable database governance guards. Five TEST applications and eight synthetic applicants now cover individual, GCC future-travel, mixed family, NOT_RESEARCHED and RULE_CONFLICT routes. Every application-scoped customer flag remains OFF because the unified family/travel/requirements/review runtime and browser-authentication evidence are not yet complete. PM2 `tashira-staging` is online and local/public Staging health is HTTP 200; Production and main/master remain unchanged.

- Five idempotent isolated Dynamic Interview application fixtures are ready for Staging: individual, GCC future-travel, four-member mixed-nationality family, NOT_RESEARCHED and RULE_CONFLICT. They use synthetic `.invalid` contacts, zero-value unpaid TEST records, trusted ordered applicant rows and dedicated `STAGING_TEST_*` routes; any reference/content collision fails closed. The fixture refuses non-Staging path/database identities and explicitly refuses to proceed if an application-scoped customer flag was prematurely enabled. Focused fixture tests, TypeScript and focused ESLint pass. Customer flags remain OFF pending completion of the family/travel/review runtime gate.

- A fail-closed governed Dynamic Interview rule fixture is ready for isolated Staging. It refuses any database other than `tashira_staging` or path other than `/var/www/tashira-staging`, labels its source `STAGING_TEST_SYNTHETIC_NOT_REGULATORY`, and creates only six minimum synthetic rule versions for normal, GCC, mixed-nationality and conflict coverage. Each version enters as DRAFT, proceeds through UNDER_REVIEW and an append-only APPROVED review, then activates through the existing database governance guards; no rule is represented as UAE regulatory evidence. Focused fixture tests, TypeScript and focused ESLint pass. The script has not yet been executed, no TEST customer/application flag is enabled, and Production/main/master remain unchanged.

- Dynamic Interview answer correction is now append-only and customer-owned: the API permits edits only for an answer already present in the authenticated application, requires an explicit correction reason, and reuses the persistent history repository rather than overwriting evidence. Current eligibility now consumes only answers still relevant to the deterministic question set, so a correction such as GCC Resident YES → NO prevents the old GCC Country answer from affecting the current result while preserving it historically. Focused API/service tests pass 8/8, TypeScript and focused ESLint pass, and the production Vite build passes. Full ESLint currently exposes two unrelated pre-existing React effect findings in `carousel.tsx` and `use-mobile.ts`; they were not suppressed or changed. No Staging deployment or feature activation occurred in this milestone; Production and main/master remain unchanged.

- Dynamic Interview runtime SHA `8917e3878c8818d7b0b8d68ce8bc15dfccae12da` is deployed to Staging through the canonical native path with PM2/local/public health HTTP 200. The real Catalog governance lifecycle imported 13 generic requirement definitions and 18 generic question definitions as DRAFT, then recorded REVIEW, APPROVED and ACTIVE transitions through separated proposer/reviewer/activator actors; 124 append-only governance events exist. The customer route and authenticated API are present but every customer-facing flag remains globally OFF and no application-scoped synthetic activation has occurred because Staging currently has no governed ACTIVE Visa Rule coverage or `TEST` application fixture. Production and main/master remain unchanged.

- The authenticated customer Dynamic Interview runtime is implemented behind application-scoped `DYNAMIC_CUSTOMER_APPLICATION` + `VISA_RULES_EVALUATION` gates. It verifies the existing customer application capability before loading data, derives questions only from fields used by governed ACTIVE rules, returns one current question, validates against the ACTIVE catalog, persists append-only applicant-scoped answers with ownership checks and idempotency, preserves material history, conditionally invalidates GCC-dependent current inputs, and deterministically evaluates each applicant without family leakage. A new responsive route `/apply/:referenceNumber/interview` presents a guided question flow, trusted applicant name, adaptive phase progress and customer-safe errors. The generic DRAFT staging catalog now contains 13 requirement and 18 question definitions without embedding nationality rules. Clean Node 20 ESLint, TypeScript, 507 tests (12 environment-gated skips), build, static asset verification and server bundle pass. No customer flag has been activated and this runtime is not yet deployed to Staging; Production and main/master remain unchanged.

- Catalog Governance Migration `027` passed a final rollback/reapply rehearsal in both clean and legacy disposable MySQL 8.4 databases, including both governance/answer-history tables, four catalog version columns and all append-only triggers. Staging identity was re-proven as `/var/www/tashira-staging`, database `tashira_staging`, PM2 `tashira-staging` and listener `127.0.0.1:3002`. Verified backup `/var/backups/tashira-staging/20260826T092550Z-catalog-governance-027-predeploy-verified` passed SHA, gzip and isolated restore checks with protected counts 65 applications / 76 applicants / 121 documents. Migration `027` is applied only to Staging; protected counts remain unchanged, every customer-facing Operations flag remains OFF, PM2 is online and local/public Staging plus read-only Production public health are HTTP 200. The deployed Staging application remains at pre-runtime SHA `d0f91508b25a5d5b4fb320630c841fc7f58a1bc7`; Catalog import/activation, customer API/UI deployment and scoped browser E2E remain pending. Production DB and main/master are unchanged.

- Requirement/Question Catalog governance is now implemented on the dedicated Visa Operations OS branch with deterministic DRAFT → REVIEW → APPROVED → ACTIVE → SUPERSEDED/RETIRED transitions, separated propose/review/activate permissions, optimistic concurrency, immutable submitted content, append-only evidence, and a persistent internal tRPC API that derives actor permissions server-side. Additive Migration `027` adds governance/version evidence and append-only dynamic-interview answer events without activating runtime behavior. A deterministic progressive interview foundation validates answers server-side, preserves material answer history, enforces applicant ownership, returns only the next relevant question, and fails unresolved catalog references closed. Both clean and legacy disposable MySQL 8.4 databases contain Migration `027`; DRAFT edits and submitted-content immutability passed. Clean Node 20 TypeScript and 10 focused governance/interview/API tests pass. No Staging migration/deployment or feature activation has occurred for this milestone; Production and main/master remain unchanged.

- Final V1 Requirement Catalog foundation is complete behind closed customer flags. Additive Migration `026` versions governed document/question definitions, import evidence, and immutable requirement-instance references to exact catalog/rule versions while preserving legacy raw-code rows. A MySQL provider loads only ACTIVE + APPROVED + effective definitions, rejects overlapping active versions, and excludes INTERNAL definitions from customer projection. A deterministic generic DRAFT seed defines 12 safe document and 12 question meanings without activating any requirement. Dynamic Application composition now consumes the governed catalog, preserves applicant/travel/family sharing scope and customer-safe reasons, and records material rule-version changes between Pre-Check and final evaluation. Disposable MySQL 8.4.11 clean/legacy/apply/rollback/reapply and clean Node 20 gates pass with ESLint, TypeScript, 491 tests (12 environment-gated skips), build, static assets and server bundle. Verified Staging backup `/var/backups/tashira-staging/20260826T075638Z-requirement-catalog-026-predeploy` passed SHA checks; Migration `026` is applied to `tashira_staging` and exact SHA `d0f91508b25a5d5b4fb320630c841fc7f58a1bc7` is deployed with PM2/local/public health HTTP 200. Protected counts remain 65 applications, 76 applicants, 121 documents and 3 historical requirement instances. Catalog tables intentionally remain empty until governed import/review; `DYNAMIC_REQUIREMENTS` is globally OFF and no Customer Pre-Check/Dynamic Application/Portal flag exists, so no customer feature is activated. Existing previously authorized Operations team flags were preserved unchanged. Production and main/master are unchanged.

- The Customer Pre-Check now has a real rate-limited tRPC runtime backed by a reusable MySQL Active Rule Registry reader. Customer evaluation can consume only rules that are `ACTIVE`, `VALIDATED`, non-internal, backed by a successful source snapshot and an active source; malformed active evidence fails closed. Effective-to boundaries are inclusive, unresolved profiles return `NOT_RESEARCHED`/human review, and the customer-safe response separates authority documents, conditional/ticket requirements, travel-party conditions, operational requirements and submission-timing warnings while retaining the no-approval disclaimer. `CUSTOMER_PRECHECK` remains OFF and no Staging/customer route is activated.

- The Customer Operations Portal now has a real read-only tRPC runtime endpoint backed by the canonical MySQL Operations case bundle. It requires the existing authenticated customer application capability, rejects cross-application access before case loading, requires an application-scoped `CUSTOMER_OPERATIONS_PORTAL` flag, maps only allowlisted timeline/application states, and projects applicant-scoped requirements plus current travel schedules without supplier, finance, internal reason or staff data. The endpoint is registered but the flag remains OFF everywhere and no customer route is activated. A clean Node 20 `npm ci` container passed full ESLint, TypeScript, 474 tests (12 understood environment-gated skips), build, static-asset verification and server bundling. Production, main/master, staging runtime and all external providers remain unchanged.

- Final V1 gap closure now includes complete provider-independent AI pre-screening outcomes (`PASS`, `WARNING`, `MISSING`, `MISMATCH`, `UNREADABLE`) for classification, missing fields/pages, expiry, applicant fields/document numbers and ticket passengers, always retaining HUMAN final authority. Visa Assistant grounding now prioritizes authenticated applicant, travel-party, scheduler and document evidence after approved rules and fails unknown/conflicting answers to human review. Audited human-handoff payloads, optimistic/idempotent unified Support thread controls, a complete canonical Operations email-event ledger, controlled Regulatory Change review/impact analysis, and finance-free Manager/Operations aggregates are tested. Live AI/mailbox providers, persistence/API/UI wiring, approved content and business KPI thresholds remain explicit external/owner gates; no feature is activated.
- A clean reproducible Node 20 container copied tracked source only and ran `npm ci`, full ESLint, TypeScript, 457 tests and build/static successfully at `1bf458a`. This proves the two earlier legacy lint findings came from the locally contaminated pnpm dependency tree, not the lockfile-defined application baseline.
- Provider-independent Typing Pack, Authority Query and Visa Delivery contracts now exist behind closed flags. Typing Pack generation is template-versioned, evidence-bound, checksum-protected, draft-only and rejects prohibited payment/raw-document fields. Authority queries follow an append-only controlled lifecycle and require a safe authority reference before recording a response. Visa delivery fails closed without authenticated customer access, applicant/document ownership and a passed security scan. No government procedure, owner template or provider behavior was invented; external adapters and approved templates remain explicit configuration gates.
- The Customer Operations Portal foundation now projects the canonical immutable status stream into one authenticated, finance-free customer view with applicant-scoped progress, outstanding actions, safe scheduling explanations and ordered timeline evidence. Internal reason codes and staff-only data are omitted by construction, duplicate applicant scope fails closed, and the application-scoped `CUSTOMER_OPERATIONS_PORTAL` flag remains OFF. A responsive portal component is complete but not registered on a live customer route; focused TypeScript, ESLint and 23 tests pass. No external environment or existing customer behavior changed.
- Final V1 functional gap closure has started with a closed-by-default Dynamic Customer Application and Customer Pre-Check foundation. The customer plan composes immutable eligibility-derived requirements, applicant-scoped questions/uploads, explicit requirement classifications, travel groups and customer-safe submission scheduling without cross-applicant leakage. The review component visibly separates family members and their documents. Public pre-check results are guidance-only, preserve rule-version evidence and never promise approval. New flags `DYNAMIC_CUSTOMER_APPLICATION`, `CUSTOMER_PRECHECK` and `CUSTOMER_OPERATIONS_PORTAL` remain OFF; no live route, database migration, Staging activation, Production, payment, pricing, invoice, Stripe or Resend behavior changed. Focused TypeScript, ESLint and 28 tests pass.

- Upcoming Submissions now has an internal staff route and finance-minimized UI backed by the canonical queue. Each permitted item shows application/travel group, visa route, travel/target/latest-safe dates, countdown, scheduler category, assignment, blocking reason and the latest persistent scheduler alert; alert actions remain centralized in the scoped Case Workspace rather than duplicated. Exact SHA `045419feeffbb66a26e3f5cb360612413b7cb982` is deployed to Staging. A Team 13 runtime read returned the synthetic case with its current `RESOLVED` alert, scheduler state and queue category without finance/storage data; `/staff/operations`, local/public Staging and read-only Production public health are HTTP 200. The route remains protected by the existing Staff guard plus backend Read Model/Scheduler flags and case RBAC, with no customer-facing exposure.
- Scheduler Alert runtime/API and Staging E2E are complete. Deterministic queue conditions create one logical alert through an internal-only reconciliation service; staff routes expose list/get/acknowledge/resolve only and reload authoritative ownership/type/severity metadata under lock. New verified backup `/var/backups/tashira-staging/20260825T145858Z-scheduler-alert-025-predeploy` passed SHA, gzip and isolated restore checks before Migration `025` created two tables, four append-only triggers and three unique indexes on `tashira_staging`. Exact SHA `34e99b0aa238a3154147d7306102a6db6502db7f` is deployed to Staging. Synthetic Team 13 runtime/API/MySQL E2E passed CREATED → ACKNOWLEDGED → RESOLVED, stale concurrency rejection, idempotent replay, wrong-team denial, audit evidence and finance isolation; the internal due-soon alert correctly emitted no customer communication. Protected counts remain 65/76/121, the document fingerprint is unchanged, browser-equivalent customer/legal/auth routes and Staging/Production public health are HTTP 200. TypeScript, ESLint, 436 full-suite tests, real MySQL lifecycle persistence, build and static assets pass. No Production, main/master, Stripe, Resend, pricing, payment or invoice change occurred.
- Scheduler alert persistence now has a MySQL-backed provider with application/travel-group/schedule ownership checks, trusted server-side RBAC, Team scope enforcement, finance-minimized reads, deterministic active-alert deduplication, append-only lifecycle evidence, persistent command hashes/idempotency and optimistic version conflicts. The corrected pre-Staging migration `025` explicitly records optional applicant ownership, severity/category, correlation/idempotency identities, millisecond lifecycle timestamps and acknowledgement/resolution actors. Disposable MySQL 8.4.11 clean/legacy apply/rollback/reapply and a real persistence E2E pass, including wrong-team denial, audit evidence, duplicate suppression, stale-write rejection and finance isolation. Migration `025` remains unapplied to Staging until the alert API/runtime adapter is connected and reviewed.
- Scheduler runtime gap closure now includes deterministic customer-safe contracts for scheduled, too-early, ready, action-required and submitted states; canonical transition-derived communication events; and an optimistic-concurrency alert lifecycle (`CREATED → ACKNOWLEDGED → RESOLVED`) with duplicate suppression. Additive migration `025` models immutable alert and communication evidence with idempotency keys and four no-update/no-delete triggers. MySQL 8.4.11 clean and legacy apply/rollback/reapply rehearsals pass. Migration `025` is not applied to Staging yet and these new persistence tables are not activated in any external environment.
- Travel Party and Submission Scheduler runtime are now deployed at exact Staging SHA `996fc8b65a84602d648a6e95802cd96e56716845` after verified backup `/var/backups/tashira-staging/20260825T124842Z-travel-scheduler-runtime-predeploy-verified`. Both flags are enabled only for synthetic Teams 7 and 13; no global/customer/Production scope is enabled. Synthetic Team 13 E2E proves one two-member travel group, one shared booking linked explicitly to both applicants, immutable current scheduler evidence, `FUTURE` Upcoming Submissions classification, finance/storage non-disclosure and wrong-team denial. Staging local/public and read-only Production public health remain HTTP 200. Controlled Writes retain their prior Team 7-only scope; Production and main/master remain unchanged.
- Travel Party / Submission Scheduler runtime integration extends the finance-minimized Operations Case Read Model with applicant-scoped travel groups, companions, planned dates, ticket state, explicit shared-booking links, current immutable scheduler evidence and prior scheduler history. The read-only Case Workspace renders dedicated Travel Party and Submission Schedule sections. A server-side Upcoming Submissions queue uses configurable operational thresholds, enforces case RBAC per row, fails closed unless both Read Model and Scheduler flags are enabled, and never selects finance/payment/storage fields. Independent runtime gates suppress all travel-party data unless `TRAVEL_PARTY_ENGINE` is enabled and suppress scheduler current/history evidence unless `SUBMISSION_SCHEDULER` is also enabled. Focused provider, RBAC, queue, UI and flag-isolation tests pass.
- The final V1 completion program now includes inactive Travel Party and Submission Scheduler foundations on `codex/visa-operations-os-v1`. Deterministic rule precedence includes Travel Party, Ticket/Travel and Submission Timing overlays before the Operational overlay. Family cases can model multiple independent travel groups, shared ticket documents remain explicitly applicant-linked with cross-application ownership rejection, and scheduler evidence keeps official entry-validity evidence separate from operational lead-time policy. Rule-triggered travel questions ask only what active rules require and remain applicant-scoped; provider-neutral ticket pre-screening compares extracted passengers with authoritative applicant names while never deciding eligibility. Migration `024` models travel groups, shared-document links and immutable schedule snapshots, including explicit entry-validity, stay-duration and operational-policy versions. MySQL 8.4.11 clean/legacy/full-chain rehearsals and rollback/reapply passed. After verified Staging backup `/var/backups/tashira-staging/20260825T120604Z-travel-scheduler-024-preflight-verified`, migration `024` was applied to `tashira_staging` and exact SHA `a459f5da805e9bd8102fffd7914c156e44e8fb29` was deployed through the canonical native path. A synthetic case persisted one travel group, two isolated members, one shared booking linked twice and one immutable scheduler snapshot. Existing application/applicant/document counts and the document manifest stayed unchanged; Staging and Production health remained HTTP 200. `TRAVEL_PARTY_ENGINE` and `SUBMISSION_SCHEDULER` remain OFF, so no customer behavior is activated. Production, main/master, Stripe, Resend, pricing, payment and invoice behavior remain unchanged.
- Visa Operations OS V1: Phase A repository discovery is complete on the dedicated feature branch `codex/visa-operations-os-v1` at baseline `21288ad37f00f6c1c1b4b7df03ddc6a99a8de959`. The evidence-based architecture audit and controlled Phase B–L implementation/migration plan are documented under `docs/`. No schema, application behavior, Production system, Stripe, Resend, payment, pricing or document flow was changed.
- Visa Operations OS Phase B has started with pure, inactive foundations: a typed permission catalog, fail-closed resource-scope evaluator, supplier finance-safe projection, and a DRAFT-only visa-rule import contract that requires HTTPS source evidence and forces unresolved research to human review. Twelve focused tests pass; these modules are not wired into existing runtime routes and change no current behavior.
- Additive Phase B migration contracts `014`–`016` now define departments/teams/roles/permissions/scopes, append-only audit events, disabled-by-default feature flags, and a source-backed versioned Visa Rule Registry. Forward scripts contain no destructive data operations or cascading deletes, existing staff receive no implicit role, imported rules default to DRAFT, and explicit rollback scripts are present. These migrations have not been applied to any database.
- Phase B now also includes environment/scoped feature-flag evaluation that fails closed and deterministic rule-governance transitions. Production rule activation requires both the explicit `rule.activate` permission and a separate owner-approval gate; invalid lifecycle jumps are rejected. These controls remain inactive foundations with no existing route integration.
- Visa Operations OS Phase C has started with the owner-approved deterministic precedence contract: Base Route → Nationality → Residence → GCC → Age/Minor → Family → Operational. The pure evaluator exposes complete rule/source/version/document evidence, returns `RULE_CONFLICT` for authoritative disagreements, overlapping effective versions or non-official eligibility overrides, and returns `HUMAN_REVIEW_REQUIRED` for unresolved profiles. It uses synthetic fixtures only and remains disconnected from existing application/UI behavior.
- Eligibility evidence migration `017` and its rollback now define append-only evaluation runs, ordered matched-rule/version evidence and explicit conflict records. A deterministic SHA-256 integrity indicator covers the normalized result without persisting raw applicant profile fields. The migration is reviewed by static safety tests and has not been applied to any database.
- Immutable applicant evaluation snapshots now include complete ownership, rule/source/version, requirement, warning, precedence, re-evaluation and integrity evidence. Migration `018` adds append-only current-selection events rather than mutable pointers. The Rule Registry repository preserves historical versions, evaluation remains behind the closed `VISA_RULES_EVALUATION` flag, and the Family Engine aggregates independently selected applicant snapshots without cross-applicant rule/document leakage. No migration is applied and no current API/UI flow is connected.
- Dynamic requirement generation now reads only each family member's selected evaluation snapshot, resolves versioned catalog labels/questions, keeps answers applicant-scoped, promotes conditional documents only when that applicant's answer matches, and fails unknown document/question definitions to manual review rather than inventing content. It remains a pure inactive domain service with no customer UI integration.
- Phase D family readiness now uses explicit member states and deterministic blocking rules rather than percentages. Rule conflicts, human review, ineligibility, route incompatibility, and incomplete requirements block submission; legitimate visa-not-required/visa-on-arrival outcomes remain visible and nonblocking. Migration `019` additively models append-only relationship, requirement, and readiness evidence; a conservative legacy adapter keeps old family records readable without inventing relationships. Both `FAMILY_ENGINE` and `DYNAMIC_REQUIREMENTS` must be explicitly enabled, and remain closed by default. No migration has been applied and no API/UI behavior is activated. TypeScript, ESLint, build/static verification, and all 298 tests pass.
- The Phase D persistence adapter now derives the current family graph from immutable relationship events and keeps requirement instances bound to one applicant evaluation. Requirement progress and corrections append chronological events; re-evaluation creates a separate instance set and cannot rewrite prior evidence. This remains an in-memory inactive contract aligned to unapplied migration `019`.
- The Operations read-only gate now assembles one applicant-isolated case model containing current/history evaluation evidence, rule versions, re-evaluation changes/reasons, requirements, documents, family blockers/actions, relationships, permitted supplier identity and audit-safe history. Assigned/team RBAC is fail-closed, legacy cases are explicitly not evaluated, operational roles cannot receive supplier costs, and `OPERATIONS_CASE_READ_MODEL` remains closed by default. The future write/action layer is documented only; no mutation, route or UI action exists. TypeScript, ESLint, build/static verification, and all 308 tests pass.
- A visible read-only Operations Case Workspace component now presents Case Overview → Applicants/Relationships → Requirements → Documents → Evaluation History → Family Readiness/Actions → Timeline → Supplier. It contains no buttons or mutation callbacks, renders nothing while the flag is closed, explicitly labels legacy cases, and allowlists supplier operational fields so cost/margin values cannot render. It is not registered on a live route and uses synthetic render tests only. Vitest now includes React `.test.tsx` files; all 313 repository tests pass with TypeScript, ESLint, build and static-asset verification.
- The controlled-write domain layer now models Human Review → Document Review → Assignment/Claim/Reassignment → enumerated Status Transitions → Re-evaluation Requests behind the closed `OPERATIONS_CONTROLLED_WRITES` flag. Every command requires a server actor, permission/scope, expected case/document/evaluation version, valid prerequisites, reason, per-application idempotency and immutable audit evidence; finance fields cannot change. Re-evaluation appends and selects a new snapshot while preserving the prior one. No HTTP mutation, UI control, database adapter, migration application or feature activation exists. TypeScript, ESLint, build/static verification, and all 326 tests pass.
- The controlled-write persistence/API gate adds unapplied additive migration `020`, append-only action/idempotency evidence, optimistic case versions, a transaction coordinator that atomically couples the business mutation, version, business event, audit evidence and idempotency result, and five strict internal mutation contracts. The router rejects client role/scope/permission fields, derives the actor server-side, and fails closed before actor resolution while `OPERATIONS_CONTROLLED_WRITES` is disabled. No write UI exists and the default runtime adapter remains deliberately unavailable. Static migration/order/rollback checks and transactional/API tests pass; execution against a disposable MySQL database remains required before any future staging migration because this workstation currently has no authorized disposable MySQL runtime. No migration has been applied.
- Local disposable MySQL 8.4 rehearsal established the safe baseline for schema migrations `014–021`, using clean, legacy, upgrade-with-existing-evaluation and full-application databases bound only to `127.0.0.1:33306`. It found and fixed an InnoDB oversized URL index in `016`; additive `021` prevents direct ACTIVE rule imports, requires approved review for activation, freezes Rule Version evidence and protects source/review history. Later milestones below extend this verified chain through `023`. No Production/staging connection, migration, deployment or feature activation occurred.
- The default Operations write router uses a production-shaped MySQL access provider for trusted server-session identity, active role/permission grants, resource scopes and environment/scoped feature flags. Unknown grants are ignored, missing/malformed/provider-failure flags remain disabled, client role/staff headers cannot elevate access, and access-provider errors are sanitized. Unit/router checks and a disposable-MySQL fixture prove persisted team-scoped authorization; the later executor milestone below completes this runtime wiring while all flags remain closed by default.
- Rule Registry migration `022` closes the authoritative `rule layer` persistence gap without editing historical migrations or fabricating legacy meaning. New Rule Versions require one of the seven established precedence layers; the layer is immutable, while legacy rows remain explicitly unresolved and cannot be approved or activated until corrected through a separately reviewed path. Clean and legacy disposable MySQL rehearsals pass through `014–022`, and the complete synthetic persistence rehearsal remains green. No external database was contacted.
- The Persistent Executor + Internal API Gate is complete behind the closed `OPERATIONS_CONTROLLED_WRITES` feature flag. Five approved actions now use the existing deterministic domain contracts with trusted MySQL RBAC, applicant/document ownership checks, optimistic case/document versions, explicit assignment capacity, restart-safe idempotency, immutable action/audit evidence and transaction-wide rollback. Re-evaluation appends a new evaluation, rule-match and selection history without rewriting prior snapshots. Clean/legacy migrations `014–023`, 8 real MySQL executor/API tests and the full 365-test suite pass with both MySQL integrations enabled. The Controlled Write UI remains intentionally unstarted and inactive; no external database, staging, Production, main/master, Stripe, Resend, pricing, payment or invoice behavior changed.
- The Controlled Write UI final gate is now complete behind the still-closed `OPERATIONS_CONTROLLED_WRITES` flag. The Operations workspace can consume a minimized server-authoritative capability model and expose only approved Human Review, applicant-scoped Document Review, Assignment/Claim/Reassignment, state-machine Status Transitions and immutable Re-evaluation Requests. Every form requires a reason and explicit confirmation, preserves one idempotency key for an unchanged retry, refreshes canonical state after success/conflict, and never treats hidden UI controls as authorization. Legacy cases remain explicit, finance fields are absent, and no live route or flag is activated. TypeScript, ESLint, the production build/static checks, 15 focused UI/API tests, 9 persistent executor/API tests and the full 375-test suite pass against disposable local MySQL 8.4.11 databases migrated through `014–023`. No migration was applied outside the disposable local environment and no external system changed.
- The Visa Operations OS V1 final local acceptance gate is complete with a four-person mixed-nationality synthetic family. Applicant-isolated evaluation/requirements/documents, missing and replacement documents, manual review, deterministic family readiness/recovery, immutable Rule Version A→B re-evaluation, assignment, controlled status, concurrency, idempotency, RBAC, legacy compatibility and finance isolation all pass. A sanitized rendered evidence page and a non-executed staging migration/activation runbook are under `docs/`. The full suite is now 383/383 across 83 files, including 9 real persistent MySQL executor/API tests; TypeScript, ESLint, Vite/static and server bundle gates pass. Both disposable local databases were returned to zero enabled Operations flags. Staging and Production were not contacted or changed, and no migration, deployment or feature activation occurred outside local disposable MySQL.
- The Operations Case Read Model now has a production-shaped, read-only runtime integration on `codex/visa-operations-os-v1`: a finance-minimized MySQL provider, staff-session-only tRPC query, server-side role/scope/feature-flag enforcement, and protected `/staff/operations/:referenceNumber` workspace. The provider never selects storage paths, payment/Stripe data, supplier costs, margins or profits; legacy cases remain `LEGACY_NOT_EVALUATED`, applicant ownership fails closed, and the live workspace no longer imports or renders controlled-write controls. TypeScript, ESLint, the full 381-test non-integration suite, 51 focused tests, the production build/static verifier and 11 disposable-MySQL integration tests pass. Staging deployment and restricted flag activation remain the next separately guarded actions in the owner-authorized runtime integration milestone; Production and main/master remain unchanged.
- Staging Step 2 Read Model validation is complete at deployed SHA `efdf8dbbe513be2b0ea50886137812e2c761e95c`. Browser-equivalent direct navigation and refresh serve the SPA for the protected Operations route while API misses remain JSON 404 and static assets remain isolated. Five named synthetic staff identities and two `TEST` cases prove assigned/team access, wrong-team denial, Finance-only denial, no-permission denial, mixed-family applicant/requirement isolation, immutable evaluation history, legacy behavior, supplier identity visibility and finance/storage-path non-disclosure. `OPERATIONS_CASE_READ_MODEL` is the only enabled Staging Operations flag; Controlled Writes and customer-facing flags remain off. The verified pre-bootstrap backup is `/var/backups/tashira-staging/20260825T055131Z-step2-rbac-bootstrap-verified`. Production and main/master were not modified.
- Staging Step 3 persistent-write validation was completed for synthetic Operations Team 7. A verified preflight backup exists at `/var/backups/tashira-staging/20260825T090427Z-operations-step3-preflight`; nine real MySQL tests proved Human Review, applicant-scoped Document Review, assignment, controlled status transitions, immutable re-evaluation, restart-safe idempotency, stale-write rejection, finance non-mutation and transaction-wide rollback. The scoped `OPERATIONS_CONTROLLED_WRITES` flag was subsequently disabled and remains OFF; Team 7 retains only the internal `VISA_RULES_EVALUATION` scope. Every customer-facing Operations/Regulatory flag remains off. Production and main/master remain unchanged.
- The full V1 Master-Scope Gap Audit now reports core Operations engineering at approximately 78% and the complete Master Program at approximately 61%, without counting provider contracts as live integrations. Provider-independent, inactive frameworks now cover AI document pre-screening with mandatory human authority, one canonical status projection for portal/email/assistant/timeline, fail-closed Visa Assistant grounding, audited/deduplicated Email+Chat Support Inbox contracts and a proposal-only Regulatory Watcher that can never auto-activate rules. Sixteen focused tests pass; external AI/mailbox/source adapters, approved regulatory content, Typing Pack/authority procedures, customer activation, dashboards and analytics remain explicit gaps. A non-executed Production Readiness Package is documented under `docs/`; no customer-facing flag, Production system, main/master, Stripe, Resend, pricing, payment or invoice behavior changed.
- The historical Staging hardening gate at functional SHA `87606b2a7eb8027ae05cdf9107365737a940f5df` passed TypeScript, ESLint, canonical build/static verification, 397 non-integration tests and nine persistent MySQL tests. Later exact-SHA milestones supersede that runtime evidence. Controlled Writes is now OFF; Team 7 Visa Rules Evaluation remains an internal-only scope, and all customer-facing/AI/Support/Regulatory flags remain OFF.

- Refund/security-deposit program: Phase 1 is implemented and verified end to end on isolated staging. Administrator re-authentication, duplicate-balance reservation, safe capability delivery/retry, Stripe TEST deposit collection, approved Stripe TEST refund execution, replay prevention, and immutable evidence all passed focused synthetic UAT. No Production migration, deployment, payment, refund, or data change occurred.
- Phase: Phase 9 — launch-blocker closure.
- Branch: `devops/deployment-safety`.
- Current verified implementation: final Payment Successful presentation on `devops/deployment-safety` (commit recorded in Git history).
- CI: latest refund-notification evidence commit `e0f52fc` is GREEN (`32567440238`).
- Tests: 210/210 passing across 50 files.
- TypeScript: PASS.
- ESLint: PASS.
- Build: PASS.
- Verified launch readiness: 96%.
- Classification: C — Not Launch Candidate.

## Completed capabilities

- An isolated AI Creative Factory POC now provides four reusable TASHIRA creative skills, a provider-neutral media architecture, six policy-safe A/B concepts, complete short-video storyboards/prompts, six generated key visuals, a scored review process, and a local owner gallery under `creative-lab/`. It has no social-account, ad-spend, Production, payment, pricing, Resend, or customer-data integration; final motion renders remain blocked on owner selection of a video provider.

- Refund Phase 1 now provides a unified visa-payment/security-deposit case model, available-balance protection, transparent full/percentage/fixed/actual-cost calculations, separate administrator approval and execution re-authentication, Stripe idempotency, immutable timeline/financial evidence, and an Admin Application Details workflow. This remains isolated to the review branch and is not deployed to Production.
- Security-deposit Phase 1 now supports an administrator-selected AED amount per application, a hashed 256-bit expiring customer capability, idempotent Resend delivery evidence, explicit accept/decline, an isolated Stripe PaymentIntent, authoritative amount/request verification, separate PAID evidence, and automatic refund-state integration. It remains review-branch only pending isolated staging migrations and UAT.
- Failed security-deposit email delivery can now be retried against the same request without creating a duplicate financial obligation. Each retry atomically rotates and invalidates the prior capability, uses a retry-specific provider idempotency key, records delivery evidence, and refuses concurrent or post-delivery retries. This remains review-branch only pending isolated staging migrations and UAT.
- Isolated staging migrations 009–011 are applied after a root-only database/code rollback snapshot at `/var/backups/tashira-staging/20260822T000824Z`. The canonical staging deployment passed, both Production and staging remained HTTP 200, and the staging document fingerprint remained unchanged. Synthetic UAT `TSH-DEPOSIT-UAT-1787357952121` verified one allowed-recipient deposit email, an exact AED 10.00 TEST deposit, a transparent 2% deduction, one AED 9.80 TEST refund, replay rejection, and single-instance evidence.
- Signed Stripe webhooks now finalize security deposits independently of the customer's browser return. The handler resolves ownership from the deposit request, re-retrieves the PaymentIntent, verifies exact AED amount and request metadata, records failure safely, and shares an idempotent finalizer with the customer confirmation API. Focused staging UAT `TSH-DEPOSIT-UAT-1787358318791` passed webhook finalization, duplicate-event rejection, customer confirmation replay, and the subsequent single-instance refund.
- Administrators can now safely reconcile asynchronous Stripe refund outcomes that remain `PROCESSING`. Reconciliation requires re-authentication and an explicit confirmation phrase, verifies every Stripe refund still belongs to its original PaymentIntent, preserves pending states, records final timeline/financial evidence once, and synchronizes the associated security-deposit state without duplicating a refund.
- Completed and partially completed refunds now produce one idempotent customer notification per refund case. Immutable append-only delivery evidence records final `SENT` or retryable `FAILED` attempts without updating historical rows, while a generated sent-only uniqueness key prevents duplicate successful delivery. Migrations 012 and 013 are applied only to isolated staging after a verified backup at `/var/backups/tashira-staging/20260822T095902Z-refund-email`; the exact deployed commit is `e0f52fc`.
- Focused staging refund-notification UAT for `TSH-DEPOSIT-UAT-1787358318791` verified one final `SENT` evidence row with a provider reference, preserved the earlier historical `QUEUED` attempt, and returned `ALREADY_SENT` on replay. Production and staging remained HTTP 200, and the staging document fingerprint remained unchanged.

- Production test data can now be retained as immutable evidence while being excluded from normal operations through an explicit `LIVE`/`TEST` application classification. Administrative and staff application lists, headline analytics, invoices/VAT consumers, and the finance cockpit filter to `LIVE` records server-side. Migration 009 is non-destructive and defaults every new application to `LIVE`; marking the inventoried pre-launch Production applications as `TEST` remains a separately authorized guarded database operation after deployment.
- Stripe runtime mode is now explicit and fail-closed: the protected workflow maps `PRELIVE` to `TEST` and `LIVE` to `LIVE`, boot rejects incomplete or mixed credentials, LIVE webhook signatures/events are accepted only in LIVE mode, and the existing three-event allowlist, idempotency, amounts, and payment finalization behavior remain unchanged. Local gates passed with 181 tests and a production build; no LIVE credential was configured and no payment occurred.
- Isolated native staging with separate checkout, MySQL database/user, filesystem storage, logs, port, and PM2 process.
- Migration 005 reviewed, hardened, applied only to `tashira_staging`, and verified with 16 append-only triggers.
- Server-authoritative versioned pricing and immutable price snapshots.
- Single and multi-applicant canonical application model.
- Filesystem document upload with ownership checks and lifecycle/timeline evidence.
- Signed customer application capability, admin/staff authorization foundations, risk engine, evidence manifest, finance/VAT cockpit, retention, and legal holds.
- Stripe TEST-only verification/webhook foundation and transactional-email/recovery abstractions.
- Safe dependency updates reduced the audit from 24 to 16 findings without force-upgrading incompatible packages. The direct `@hono/node-server` dependency is locked to the patched `1.19.17` release.
- Router packages are aligned on React Router 6, eliminating the verified `/staff` fallback crash.
- Route-level lazy loading reduced the main client chunk from about 3.4 MB to about 1.19 MB.
- The chatbot now sends canonical visa-service codes, links to the registered `/pay/:referenceNumber` route, and displays the server-authoritative quote.
- Family chatbot applications use stable applicant IDs and zero-based slots, isolate every document by applicant, resume from server state using a reference-only browser marker, and submit one aggregate server-authoritative quote.
- Staging migration 006 enforces unique `(application_id, applicant_index)` slots and refuses automatic duplicate resolution. Synthetic API UAT verified two applicants, six isolated documents, cross-slot rejection, resume data, and a USD 340 server quote.
- Privileged document lifecycle APIs now accept a trusted document ID instead of a client-provided storage path. Replacement uploads and validates the new file before removing the old one, updates MySQL metadata, and preserves immutable lifecycle references.
- Exact-commit staging UAT authenticated as an administrator and verified original signed-URL access, replacement content, logical deletion, refusal to sign a deleted document, and removal from active document lists.
- A permanent staging authentication verifier now confirms anonymous admin rejection, admin login/session/logout, synthetic staff creation, staff login/session verification, staff-only API access, logout invalidation, and cleanup without exposing credentials or tokens.
- Public staging is live at `https://staging.tashiraev.com` with a dedicated Let's Encrypt certificate, HTTP-to-HTTPS redirect, noindex/security headers, and a staging-only Nginx proxy to `127.0.0.1:3002`. External health and unauthenticated route-guard browser smoke tests pass.
- Synthetic browser UAT verified staff login and the real staff dashboard through public HTTPS. The temporary staff account was removed. Automated activation of the dashboard logout control was inconclusive, so no speculative application change was retained.
- Lazy-loaded routes now recover once from stale deployment chunks, fixing admin/staff pages that previously failed when a browser retained an older entry bundle. Unrelated errors are never swallowed, and four regression cases cover the classifier and loop prevention boundary.
- In-place builds now retain prior content-hashed chunks so tabs opened before deployment can finish lazy imports. HTML/client routes are `no-store`, hashed assets are immutable, every generated local asset reference is verified during `npm run build`, and an explicit chunk-load fallback replaces the raw router error screen.
- Staging restored the exact previously missing `AdminApplications-1JinaxRj.js` generation, served 172 retained JavaScript assets without HTTP errors, and passed authenticated browser UAT for `/admin`, applications, a synthetic application detail, suppliers, staff management, invoices, supplier bills, VAT, finance, chat, public routes, hard refresh, and normal navigation.
- Stripe TEST keys are wired through ignored staging-only secret files with TEST-mode startup enforcement. Migration 007 adds durable webhook event claims; signed duplicate delivery is replay-safe.
- Synthetic Stripe TEST UAT passed success, decline, retry, 3DS-required, abandonment, valid/invalid signature, and duplicate-delivery scenarios. Snapshot, Stripe, payment, and invoice amounts matched; payment/invoice evidence remained single-instance.
- Payment creation now fails closed unless a server-authoritative readiness evaluator confirms required application data, current policy acceptance, a valid price snapshot, every applicant's required personal/passport fields, and flow-specific applicant-owned documents. Primary-form uploads occur before checkout. Incomplete and partial staging applications returned HTTP 412; a complete application received a Stripe TEST client secret; an incomplete family member blocked aggregate payment without cross-application data disclosure.
- Stripe TEST 3DS UAT for `TSH-372754` is blocked by the configured AE Stripe account reporting `charges_enabled: false` and `card_payments: inactive`. Redacted diagnostics confirmed matching TEST publishable/secret keys, an allowed `card` PaymentIntent, no server-side payment attempt/error, and working PaymentIntents 3DS semantics with Stripe's predefined authentication-required PaymentMethod. No code workaround or staging deployment was performed; the account owner must resolve the Stripe capability requirement before interactive 3DS retest.
- Verified payment success now unmounts checkout, refreshes canonical application state, and renders one Payment Confirmed experience. Paid applications remain on that state after refresh/back and cannot create another PaymentIntent. Stripe TEST webhook replay remained single-payment/single-invoice.
- Customer checkout now performs the authoritative readiness query before mounting payment UI. Incomplete applications remain in the form with grouped applicant requirements and a Complete Documents focus action; Stripe loading is deferred until the ready-only payment component mounts. Staging browser UAT verified no Secure Payment modal and zero Stripe card frames for an incomplete application.
- Payment-success views now discard the retained checkout scroll position and focus the programmatic success heading without causing a second scroll. The behavior applies both to the primary-form confirmation and the canonical paid payment page used after confirmation, 3DS return, refresh, and revisit.
- Staging transactional email delivery is accepted by Resend and was confirmed in the approved `admin@tashiraev.com` inbox using the sandbox sender. Provider-independent Magic Link and Email OTP recovery now issue hashed, expiring, single-use challenges behind enumeration-safe responses and rate limits; successful verification grants only the recovered application capability and routes to its canonical state.
- Outbound email evidence now records Magic Link and Email OTP under their exact template identities; migration 008 extends the append-only evidence enum without weakening its update/delete protections.
- Staging commit `04247b4` now loads only allowlisted non-secret mail settings from ignored staging configuration and the Resend key from its mode-600 secret file. Migration 008 is applied to `tashira_staging`; `/recover`, enumeration-safe unknown-email handling, and invalid-token rejection were reverified without sending mail.
- Authorized synthetic recovery UAT created an application through the canonical API, sent one Magic Link and one Email OTP to the sole approved staging recipient, and recorded two `SENT` evidence rows under the exact `RESUME_LINK` and `RECOVERY_OTP` templates without exposing either credential.
- Recovery email rendering now includes a safe clickable HTML action and clickable fallback URL while retaining the plain-text alternative; only the exact HTTPS staging recovery origin is accepted.
- Interactive staging Recovery UAT passed end to end: the approved inbox received the HTML Magic Link, the active `Resume Application` action opened the correct canonical payment screen, and reuse of the same link was rejected without granting a new session.
- The canonical recovery payment page now performs the same server-authoritative readiness gate as the primary form. Incomplete applications show grouped requirements and a `Complete Application` action instead of raw API JSON or card entry; Stripe uses the pure loader and is not initialized until readiness is `READY`. Staging browser UAT verified zero Stripe frames/elements for an incomplete application and confirmed that the completion action opens the assistant at the persisted resume step.
- Public customer tracking UAT now verifies the application reference, immutable timeline, and canonical `/pay/:referenceNumber` continuation route. The footer's Track Application link now opens `/track` instead of a dead fragment.
- Staff logout now awaits server-side session invalidation before navigation while retaining a guaranteed local cleanup path. The existing authenticated browser click still requires a final manual confirmation; no synthetic credential was entered without owner confirmation.
- Authoritative Payment Successful email delivery now runs after verified Stripe finalization, uses Resend idempotency keys and durable evidence, remains failure-isolated from payment truth, and keeps production delivery disabled until explicitly enabled.
- Fake Google IDs were removed. Verified purchase conversion now uses the server-confirmed amount/currency and stable reference, emits only after successful backend confirmation, and remains disabled until owner IDs are configured.
- Unpaid applications are now rejected server-side from operational processing states. The rule is covered by eight regression cases.
- Final V1 legal-policy remediation is deployed to isolated staging: complete equivalent English/Arabic Terms, Privacy, and Refund/Cancellation policies; explicit three-policy acceptance in the primary form and chatbot; immutable policy bundle `legal-bundle-2026-08-18-v1`; auditable `PROCESSING_STARTED` transition; qualified timing/private-provider wording; and working `/terms`, `/privacy`, `/refund`, `/contact`, and `/track` direct/hard-refresh routes. Exact-commit gates passed with 112 tests, the merged staging tree passed 120 tests and build, and CI run `32121645748` is green at `94ad1ff`.
- The owner-approved final Payment Successful presentation now uses one shared normal/chatbot component, a centered responsive desktop layout, the existing authorized inline PDF invoice route, direct secure download, paid-state tracking round-trip, clean home exit, and authoritative primary-applicant identity for newly generated invoice PDFs. Focused tests and all local quality gates pass without changing Stripe, pricing, webhook, Resend, or email behavior.
- The final V1 invoice now maps every BILL TO identity field from ordered Applicant 1, preserves full-family financial evidence from the immutable price snapshot and verified payment, and embeds the OFL-licensed Noto Naskh Arabic font for shaped RTL Arabic values without changing the English/LTR structure. Preview, download, admin access, and email attachments continue to use the same canonical server PDF.
- Checkout now separates the lead applicant from an explicitly authorized payer/cardholder, records a versioned idempotent payer-authorization timeline event, supports authorized third-party relationships, exposes safe payer evidence to Admin and chargeback manifests, and never stores card-entry data. The invoice BILL TO, server-authoritative amount, Stripe lifecycle, and applicant ownership remain unchanged.
- Canonical invoice PDFs now preserve the lead applicant under BILL TO and show the separately recorded payer under PAYMENT DETAILS across preview, download, secure email access, attachment, and Admin view. Relationship and safe Stripe card brand/last4 appear only when available; payment and chargeback evidence use the same immutable payer event without storing sensitive card data.
- Legal bundle `legal-bundle-2026-08-19-v2` now states the approved English/Arabic refund-deduction principle: only actual, disclosed and legally permitted costs may reduce an eligible refund; calculations must be transparent; duplicate/incorrect payments carry no discretionary penalty; and refundable security deposits remain separately protected. Automated refund accounting remains post-launch and unimplemented.

## Active blockers

- Interactive browser completion of the 3DS challenge and a future reviewed Payment Element migration for conditional provider-requested billing fields. Automated TEST verification reached the expected `requires_action` state, but the interactive challenge was not completed in this phase.
- Final V1 candidate `TSH-V1-1786901526429` reached READY with three documents but Stripe TEST confirmation failed before webhook completion; the fresh paid E2E remains blocked by account capability.
- Manual authenticated browser confirmation of the staff logout control. The implementation and automated session tests pass, but the browser interaction still requires explicit approval before entering a synthetic password.
- Owner-approved business configuration.
- Remaining npm audit findings requiring upstream, major-version, or package-replacement decisions.
- Large XLSX/invoice chunks and the 35.1 MB bundled server artifact.

## Owner decisions/actions required

- Approve production pricing/company/VAT/exchange-rate/invoice values.
- Review any production credential rotation and migration plan as separate authorized changes.
- Resolve Stripe TEST card-payment capability, confirm the Resend domain/sender in its dashboard, and provide Google Tag/Ads conversion IDs plus the purchase label.

## Next highest-priority task

Add the internal read-only Rule Registry history projection and Operations UI for Draft/Review evidence behind the still-closed `REGULATORY_WATCHER`; it must expose source governance and immutable lifecycle history without enabling mutations or inventing official rule content. Production merge, migrations, deployment, official rule/catalog content, external AI/mailbox providers, retention/legal policy and any feature activation remain true owner gates.
